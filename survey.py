import numpy as np

W1 = np.array([
    [-1.77, -.16, -.97, -.35, -.4, -.06, 5.08, -.64, .06, -.02, .18, .18, -.53, -.14, .97, -1.12, .2, .31, .74],
    [-.13, -.01, .15, -.12, .15, -.07, 0, 0.1, -.24, -.17, -.09, -.11, .16, -.18, -.14, -.18, -.25, -.17, .16],       
])

B1 = np.array([
    [2.15],
    [0]
])

W2 = np.array([
    [-.56, -.22], 
    [1.71, -.59]
])

B2 = np.array([
    [1.53],
    [-1.05]
])

x = np.array([
    [0.73],
    [0.92],
    [0.50],
    [1],
    [0.12],
    [0.04],
    [0.01],
    [0.05],
    [0.10],
    [0.08],
    [0.01],
    [0],
    [0.53],
    [0.83],
    [0.84],
    [0.34],
    [0.77],
    [0.89],
    [0.86],
])

def ReLU(x):
    # for each element of x, if it is less than 0, return 0, else return the element
    return np.maximum(x, 0)

H = np.dot(W1, x)
H = np.add(H, B1)
H = ReLU(H)

O = np.dot(W2, H)
O = np.add(O, B2)

print(O)